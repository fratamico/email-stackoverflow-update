In conjunction with the following cron job, will check a given stack overflow site and send specified user an email when the first reply has been posted (checks every 5 minutes).

*/5 * * * * python /home/location_of_file/searchWeb.py >/dev/null 2>&1
